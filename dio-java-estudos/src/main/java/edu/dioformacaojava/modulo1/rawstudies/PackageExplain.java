package main.java.edu.dioformacaojava.modulo1.rawstudies;

public class PackageExplain {
    /*
    O package é uma forma de organizar as classes em um projeto Java. Ele permite agrupar classes relacionadas 
    em um mesmo diretório, facilitando a manutenção e a reutilização do código.

    A convenção de nomenclatura para pacotes é usar letras minúsculas e separar palavras compostas com pontos. 
    Por exemplo, o pacote "edu.dioformacaojava.modulo1" indica que a classe está localizada no diretório "modulo1", 
    que está dentro do diretório "dioformacaojava", que por sua vez está dentro do diretório "edu".

    O package também é usado para evitar conflitos de nomes entre classes. Por exemplo, se duas classes tiverem o mesmo 
    nome, mas estiverem em pacotes diferentes, elas podem coexistir no mesmo projeto sem problemas.
    
    Para usar uma classe de outro pacote, é necessário importá-la usando a palavra-chave "import". Por exemplo, para usar a 
    classe "ClassAnatomy" do pacote "edu.dioformacaojava.modulo1", 
    é necessário escrever "import edu.dioformacaojava.modulo1.ClassAnatomy;" no início do arquivo.

    ------------------------------------------------------------------------------------------------------------------------------

    O .com, .org, .net, .gov, .edu são domínios de nível superior (TLDs) usados na internet para 
    identificar a finalidade ou a origem de um site.

    O .com é usado para sites comerciais, e é o TLD mais popular e amplamente utilizado na internet. 
    Ele é usado por empresas, lojas online, blogs e outros tipos de sites comerciais.

    O .gov para sites governamentais é usado para identificar sites oficiais do governo. 
    Ele é restrito a agências governamentais e departamentos oficiais.

    O .net é usado para sites relacionados a redes e tecnologia, como provedores de serviços de internet, 
    empresas de tecnologia e sites de hospedagem.

    O .edu é usado para sites educacionais, como universidades e escolas. Ele é restrito a instituições educacionais credenciadas e 
    é usado para identificar sites que oferecem recursos educacionais.

    O .io é usado para sites relacionados a tecnologia e startups, especialmente na área de desenvolvimento de software.
    Ele é popular entre empresas de tecnologia e startups, e é usado para identificar sites que oferecem 
    serviços e produtos relacionados a tecnologia.

    O .org é usado para sites de organizações sem fins lucrativos, como ONGs, fundações e associações. 
    Ele é usado para identificar sites que promovem causas sociais e comunitárias.

    ------------------------------------------------------------------------------------------------------------------------------

    As convenções mais utilizadas para nomear pacotes são:
    utils: para classes utilitárias, como funções de manipulação de strings, datas e arquivos.

    model: para classes que representam modelos de dados, como entidades de banco de dados ou objetos 
    de transferência de dados (DTOs).

    service: para classes que implementam a lógica de negócios do aplicativo, como serviços de autenticação, 
    envio de e-mails e processamento de pagamentos.

    controller: para classes que lidam com a entrada do usuário e a saída do aplicativo, 
    como controladores de interface do usuário e manipuladores de eventos.

    repository: para classes que lidam com a persistência de dados, como repositórios de banco de dados e arquivos.

    test: para classes de teste, como testes unitários e de integração.

    ------------------------------------------------------------------------------------------------------------------------------

    A convenção de nomenclatura para pacotes é usar letras minúsculas e separar palavras compostas com pontos.
    Por exemplo, o pacote "edu.dioformacaojava.modulo1" indica que a classe está localizada no diretório "modulo1", 
    que está dentro do diretório "dioformacaojava", que por sua vez está dentro do diretório "edu".
    */
}
